# Stale Check Organizer — Dev Notes

## Rebuild the desktop app

After making changes, rebuild the `.exe` with:

```bash
cd "C:\Users\owenc\OneDrive\Documents\Stale Check Organizer"
npm.cmd run pack
```

The new build will appear in the `release\` folder.
